import 'dashboard_screen.dart';

/// Deprecated: use [DashboardScreen] via [TabsManager] shell.
@Deprecated('Use DashboardScreen via TabsManager navigation shell')
class HomeScreen extends DashboardScreen {
  const HomeScreen({super.key});
}
